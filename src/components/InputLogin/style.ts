import { StyleSheet } from "react-native";

export const styleInput = StyleSheet.create({
  input: {
    width: "80%",
    height: 40,
    backgroundColor: "#fff",
    borderRadius: 5,
    paddingHorizontal: 10,
    marginBottom: 20,
  },
});
